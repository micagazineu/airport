const fs = require('fs');
const htmlTemplateString = require('./html-template');

const makeHtmlOutput = async (results, outputPath) => {
  const resultsJson = JSON.stringify(results, null, 2);
  fs.writeFileSync(outputPath, htmlTemplateString(resultsJson));
};

module.exports = makeHtmlOutput;
